<div class="container">
  <div class="col">
    <%= form_with scope: :remote, url: remotes_path do |f| %>
     <%= f.text_field :text %>
     <%= f.submit 'Send' %>
   <% end %>    
  </div>

  <div class="col"> 
    <% @remotes.each do |r| %>
     <%= r.text %> <br>
    <% end %>
  </div>
</div>